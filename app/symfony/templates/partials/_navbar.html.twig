<nav class="navbar">
    <div class="container">
        <div class="navbar-inner">
            <a href="{{ path('app_home') }}" class="navbar-brand">
                <img src="{{ asset('images/logo-navbar.svg') }}" alt="Passe Civique" class="navbar-logo">
                Passe Civique
            </a>

            <div class="navbar-nav">
                <a href="{{ path('app_home') }}" class="nav-link {{ app.request.get('_route') == 'app_home' ? 'active' }}">
                    Accueil
                </a>
                <a href="{{ path('app_pricing') }}" class="nav-link {{ app.request.get('_route') == 'app_pricing' ? 'active' }}">
                    Tarifs
                </a>

                {% if is_granted('ROLE_USER') %}
                    <a href="{{ path('app_quiz_start') }}" class="nav-link {{ app.request.get('_route') starts with 'app_quiz' ? 'active' }}">
                        Quiz
                    </a>
                    <a href="{{ path('app_profile') }}" class="nav-link {{ app.request.get('_route') starts with 'app_profile' ? 'active' }}">
                        Mon Profil
                    </a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_admin') }}" class="nav-link {{ app.request.get('_route') starts with 'app_admin' ? 'active' }}">
                            Admin
                        </a>
                    {% endif %}
                    <a href="{{ path('app_logout') }}" class="btn btn-secondary btn-sm">
                        Deconnexion
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="nav-link {{ app.request.get('_route') == 'app_login' ? 'active' }}">
                        Connexion
                    </a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm">
                        Inscription
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
